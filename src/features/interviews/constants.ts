export const enum InterviewPageState {
    Default = "default",
    Adding = "adding",
    Editing = "editing",
    View = "view",
}

export const enum Order {
    Ascending = "asc",
    Descending = "desc",
};

export const enum InterviewSectionFilter {
    All,
    Pending,
    Interviewed,
    NotPresent,
}